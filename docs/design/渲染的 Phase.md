## 核心原则：

### 数据读写的阶段性

每个 Phase 有特定的数据读写规则。比如：
- 某个阶段只能**读写** CPU 资源
- 某个阶段只能**读取** CPU 资源，写入 GPU 资源
- 某个阶段只能**读写** GPU 资源

这样做的好处是：
- 明确数据流向，避免资源访问冲突
- 可以将同类访问性的数据都组装在一起，使用 `RenderContext<'a>` 这样的结构体来访问，简化接口设计 
