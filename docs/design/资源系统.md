# 资源系统

【TODO】


Texture 是一个资产语义，而不是 GPU 语义。  
  
应该需要一个 TextureHandle，这个仅仅是在资源系统中使用，一定不要跑到其他系统中去了。  
特别需要和 Bindless 系统隔离开来。  
  
Texture 应该包含这些信息：  
- ImageHandle  
- ImageViewHandle  
- Sampler 信息  
- sRGB 标记  
- mip 策略  
- 加载状态  
  
Asset 系统不自己创建 ImageView，一定是通过 ImageViewDesc 拿到的，可以缓存 ImageView
