#include "share/__common.slangi"

namespace raster
{
struct PushConstants
{
    PTR(PerFrameData, frame_data);
    PTR(GPUScene, scene);

    /// 仅 raster 使用
    uint instance_idx;
    /// 仅 raster 使用
    uint submesh_idx;
    uint _padding_1;
    uint _padding_2;
};

};
