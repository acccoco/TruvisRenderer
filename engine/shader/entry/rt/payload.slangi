/// @file payload.slangi
/// @brief Re-export：光线追踪载荷结构体定义
///
/// 实际定义已移至 share/rt_payload.slangi，此文件保持向后兼容

#pragma once
#include "share/pass/rt.slangi"
#include "share/rt_payload.slangi"

/// Push Constants 声明（所有 RT 着色器共享）
[[vk::push_constant]]
rt::PushConstants push_const;

/// 路径追踪最大深度
const static uint max_depth = 16;

/// 超过一定数量后，就不再重新计算了
const static uint max_accum_samples = 4096;
