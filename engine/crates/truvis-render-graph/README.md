# truvis-render-graph

声明式渲染图模块，负责 pass 依赖编排、资源状态推导与执行计划构建。

## 提供能力

- 构建 pass DAG
- 声明图像/缓冲读写关系
- 自动生成必要同步与状态转换
- 编译后按拓扑顺序执行

## 边界约束

- 仅关注图编排，不依赖 scene/asset 等领域模块
- 业务 pass 逻辑在上层模块组织（如 `truvis-app`）
